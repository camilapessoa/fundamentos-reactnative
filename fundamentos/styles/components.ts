import { StyleSheet } from "react-native";

const components = StyleSheet.create({
    center:{
        flex:1,
        justifyContent: "center",
        alignItems: "center",
    }

})

const center = components.center;

export { center }
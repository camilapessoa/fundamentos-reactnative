import { StyleSheet } from "react-native";

const utils = StyleSheet.create({
    textXl:{
        fontSize: 24,
    },
    fontBold: {
        fontWeight: "bold",
    },
    textRed500:{
        color: "#f44336",
    },

});

const textXl = utils.textXl;
const fontBold = utils.fontBold;
const textRed500 = utils.textRed500;

export { textXl, fontBold, textRed500};